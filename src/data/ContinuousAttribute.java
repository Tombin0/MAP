// Questa classe rappresenta un attributo continuo, che può assumere valori numerici reali.
package data;

    public class ContinuousAttribute extends Attribute {
        public ContinuousAttribute(String name, int index) {
            super(name, index);
        }
    }